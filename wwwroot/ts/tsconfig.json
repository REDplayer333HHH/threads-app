{
    "compilerOptions": {
      "target": "es6",     
      "lib": ["es6", "dom"],          // Set the JavaScript version to which TypeScript should compile.
      "module": "commonjs",          // Set the module system (e.g., CommonJS, ES6, etc.)
      "outDir": "../js",            // Directory to output the compiled JavaScript files.
      "rootDir": ".",            // Directory that contains your TypeScript source files.
      "strict": true,                // Enable all strict type-checking options.
      "esModuleInterop": true,       // Ensure compatibility with ES6-style imports.
      "skipLibCheck": true           // Skip type checking of declaration files (`.d.ts`).`1
    }
  }
  